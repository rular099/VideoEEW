# Upstream CoTracker changes

No upstream CoTracker files have been changed by VideoEEW.

The adjacent local and server checkouts contain pre-existing user files and, on
the server, pre-existing tracked changes. They are excluded from VideoEEW Git
operations. Any future required upstream modification will be described here
and exported to `reports/upstream_changes.patch`.

